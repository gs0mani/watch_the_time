/*chrome.runtime.onMessage.addListenser(
	function(request, sender, sendResponse) {
		if(request.check == "is_focus")
		{
			$(window).focus(function() {
				sendResponse({answer: "yes"});
			});
			$(window).blur(function() {
				sendResponse({answer: "no"});
			});
		}
	});*/